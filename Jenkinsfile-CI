pipeline {
     agent any
     stages {
         stage('checkout') {
             steps {
                  git 'https://github.com/kliakos/sparkjava-war-example.git'
             }
         }
         stage ('Build') {
              steps {
                 sh "mvn package"
              }
         }
      }
}
